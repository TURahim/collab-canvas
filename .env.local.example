# CollabCanvas Environment Variables
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# Firebase Configuration
# =============================================================================
# Get these from Firebase Console > Project Settings > General > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id_here

# =============================================================================
# OpenAI Configuration (AI Canvas Agent)
# =============================================================================
# IMPORTANT SECURITY NOTE:
# - DO NOT add NEXT_PUBLIC prefix to this variable
# - This API key is SERVER-SIDE ONLY and must never be exposed to the client
# - The key is used by the server-side API proxy at /api/ai/execute
# - Get your API key from: https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-your_openai_api_key_here

# Cost Estimate:
# - GPT-4 Turbo Preview: ~$0.01-0.03 per command
# - Rate limited to 10 commands per hour per user
# - Monitor usage at: https://platform.openai.com/usage

# WARNING:
# - Never commit .env.local to git (it's in .gitignore)
# - Never expose OPENAI_API_KEY to the client browser
# - The server-side proxy architecture ensures the key stays secure
# - If the key is ever compromised, rotate it immediately at OpenAI platform

# =============================================================================
# Development Notes
# =============================================================================
# - After updating .env.local, restart your development server
# - For production deployment (Vercel), add these variables to your project settings
# - Test the AI API health check at: http://localhost:3000/api/ai/execute
